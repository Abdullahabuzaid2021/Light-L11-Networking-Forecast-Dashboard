# GitLab CI/CD configuration for GitLab Pages
image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r dashboard/* public/
    - echo "Deploying to GitLab Pages"
  artifacts:
    paths:
      - public
  only:
    - main
